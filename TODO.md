# 1. Support JMap, JIntMap, JArray as TypeScript class objects

```ts
const map = new JMap()
map.set('foo', 'bar')
map.get('foo')
```

# 2. Add Proxy support

```ts
const map = new JMap()
map.foo
printConsole(map.foo)
```
